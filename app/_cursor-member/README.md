# Cursor member portal (parked)

This folder is **your Cursor version** of the member app. It is parked so it does not conflict with Claude’s Vercel routes.

- **Not routed** while parked here — Next.js ignores `_` prefix folders for URLs.
- **To use again:** `mv app/_cursor-member app/member` and remove or park `app/(member-portal)`.
- **Do not delete** — all scrapbook home, calendar studio, icon sidebar work lives here.
